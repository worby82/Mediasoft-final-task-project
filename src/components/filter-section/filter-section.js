import './filter-section.scss';

const FilterSection = () => {
    const FilterSectionElement = document.createElement('section');
    FilterSectionElement.className = 'filter-section';

    return FilterSectionElement;
}

export default FilterSection;